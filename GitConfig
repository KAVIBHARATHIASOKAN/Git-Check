// In terminal
idea .

// Initialise git clone if New Project(create repository in github site)
git clone https:....

//Commit
make a commit

//
git config --global user.name "Kavi Bharathi"

//
git config --global user.email kavibharathi1k@gmail.com

// Set Editor
git config --global core.editor "code --wait"

//Open config file
git config --global -e

// if Mac
git config --global core.autocrlf input

//
git config -h